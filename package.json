{
  "name": "m365-pull",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static SPA for downloading Teams chats and call recordings from a single M365 tenant, with OneDrive sync for cross-device access.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@azure/msal-browser": "^3.20.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
