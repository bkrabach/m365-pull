# Lane s30 — recording transcript 403 ("protected mp4" app not whitelisted)

## Objective
Investigate work-tracker item **m365_pull-s30** to a NAMED verdict and, if an in-code
fix genuinely exists, implement it. This is a regression: downloading a Teams meeting
recording transcript now 403s with a SharePoint error body:
`{"error":{"code":"accessDenied","message":"For protected mp4 file, this API is only
supported for whitelisted apps, your app id 63231eb0-9b53-4342-8ac4-5209d618684e is
not whitelisted."}}`

## Exit condition
Complete when **either** the item reaches a terminal state, **or** it is conclusively
demonstrated it cannot, naming the blocker. An item ending FAIL or BLOCKED is a
residual, not a failure of the goal.

Terminal states (pick one, name it):
- **PASS** — a code alt-path implemented, `npm run typecheck` clean, committed+pushed.
- **BLOCKED-external** — confirmed the only resolution is Microsoft app-whitelisting
  (no delegated code path exists); documented with the exact app id + endpoint to
  whitelist. THIS IS THE EXPECTED OUTCOME — it is a valid completion, not a failure.
- **FAIL-<reason>** / **BUDGET** (exceeded time bound).

## Where you work — ONLY here
- Working dir: this worktree. Branch: `gb/m365ui/s30`, base `d099be1`.
- Do NOT touch the main checkout or sibling worktrees.

## Files you OWN (touch nothing else)
- `src/sources/teams-recordings.ts`
- `docs/GRAPH_API_NOTES.md` (extend §9 with your findings)
If you find a needed edit outside these, RECORD it as a residual and STOP — do not cross
into another lane's files.

## Host + capability limits (read carefully)
- This box is Linux/aarch64 with NO Microsoft auth token and NO network access to Graph
  or SharePoint. You CANNOT reproduce the 403 live. Your investigation is code-reading +
  reasoning only. Do NOT fabricate a "reproduced" result.
- The failing call is `fetchRecordingTranscripts()` in `src/sources/teams-recordings.ts`
  (~:194-209): GET `https://{spHost}/_api/v2.1/drives/{driveId}/items/{itemId}?select=name,media/transcripts&$expand=media/transcripts`
  with a SharePoint-resource token from `getSpToken()` (~:62-86, scope `{spHost}/.default`).
- The benign `/me/chats/.../messages` 403s logged "No roster access ... skipped" are
  EXPECTED and unrelated — do not chase them.
- Investigate, in order: (1) confirm the error is the app-whitelist gate vs a missing
  SharePoint scope; (2) whether any DELEGATED Graph transcript path exists (note the app
  already holds OnlineMeetingTranscript.Read.All / OnlineMeetingRecording.Read.All, but
  Graph getAllTranscripts/getAllRecordings require `meetingOrganizerUserId` and are
  organizer-scoped — a prior probe confirmed 400 without it); (3) if none, it's
  BLOCKED-external.

## Discipline
- Commit early, push always (branch `gb/m365ui/s30`). NEVER merge to main — the
  orchestrator merges.
- Time bound: if you exceed it, write DONE.json with verdict reflecting progress and stop
  (BUDGET). Do not rush or skip the commit.

## Final act — DONE.json
Add `DONE.json` to `.gitignore` FIRST, then write `DONE.json` in the worktree root:
`{lane:"s30", session_id:<your own session id>, verdict:"COMPLETE"|"BLOCKED"|"PARTIAL",
branch:"gb/m365ui/s30", head:<sha>, pushed:true|false, items:["m365_pull-s30:<state>"],
residuals:[...], pending_human:[...], suite:"typecheck:<pass|n/a>"}`

## KNOWN
- Verify with `npm run typecheck` (there is NO test suite). Worktree has no node_modules —
  run `npm install` before typecheck if you make a code change (skip if docs-only).
- If your verdict is BLOCKED-external (the likely one), a docs-only change to
  GRAPH_API_NOTES.md is the deliverable; you may set suite:"typecheck:n/a".
